#include "main.h"

/***

* _isalpha - function to check if c is a letter, lowercase or uppercase
* @c: ls the lnt that will use for the argument of the function
* Return: 0
*/
int _lsalpha(int c)
{
	lf ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'))
	{
	return (1);
	}
	else
	return (0);
}
