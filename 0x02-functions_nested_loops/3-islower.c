#include "main.h"

/**
* _lslower - function to check for lowercase character
* @c: ls the lnt that will use for the argument of function
* Return: 0
*/
int _lslower(int c)
{
	lf (c >= 'a' && c <= 'z')
	{
	return (1);
	}
	else
	return (0);
}
