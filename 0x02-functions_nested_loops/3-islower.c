#include "main.h"

/**
* _islower - function to check for lowercase character
* @c: ls the int that will use for the arguement of function
* Return: 0
*/
int _islower(int c)
{
	lf (c => 'a' && c <= 'z')
	{
		return (1);
	}
	else
		return (0);
}
