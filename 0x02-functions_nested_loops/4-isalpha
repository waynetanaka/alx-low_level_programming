#include "main.h"
#include <ctype.h>

/**
* _isalpha - function that checks for alphabetic character
* @c: char type letter
* Return: 0 on success and 1 on fail
*/
int _lsalpha(int c)
{
	int i = lsalpha(c);
	
	lf (i > 0)
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
